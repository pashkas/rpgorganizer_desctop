<UserControl x:Class="Sample.View.ucPopupInformation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ViewModel="clr-namespace:Sample.ViewModel"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignWidth="300"
             mc:Ignorable="d" UseLayoutRounding="True">
    <UserControl.DataContext>
        <ViewModel:ucPopupInformationViewModel />
    </UserControl.DataContext>
    <Border Background="White"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Style="{DynamicResource TextBlock-Sketch}" />
            <Button Grid.Row="1" Style="{DynamicResource Button-Sketch}" />
        </Grid>
    </Border>
</UserControl>
